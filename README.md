# ffmpeg-batch-generator
Born from a quick fix. I built this zero-dependency HTML tool to stop manually editing terminal scripts for batch video compression. Just open it in your browser, tweak your FFmpeg settings (paths, codecs, CRF, presets), and copy a ready-to-run, single-line command for Windows, macOS, or Linux.
